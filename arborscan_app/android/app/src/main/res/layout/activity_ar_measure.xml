<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#08111F">

    <fragment
        android:id="@+id/arFragment"
        android:name="com.google.ar.sceneform.ux.ArFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Верхняя панель с подсказками -->
    <LinearLayout
        android:id="@+id/topPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@drawable/bg_panel_rounded"
        android:elevation="4dp">

        <TextView
            android:id="@+id/tvStep"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ШАГ 1 ИЗ 5"
            android:textColor="#46E0A1"
            android:textSize="12sp"
            android:textStyle="bold"
            android:letterSpacing="0.1" />

        <TextView
            android:id="@+id/tvHint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Наведите прицел на основание (корни) дерева"
            android:textColor="#F6FAFF"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Поиск плоскости..."
            android:textColor="#9BAABD"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Прицел в центре экрана -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Вертикальная линия прицела -->
        <View
            android:layout_width="2dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:background="#46E0A1" />
        
        <!-- Горизонтальная линия прицела -->
        <View
            android:layout_width="24dp"
            android:layout_height="2dp"
            android:layout_gravity="center"
            android:background="#46E0A1" />

        <!-- Плавающий текст с результатами (прямо над прицелом) -->
        <TextView
            android:id="@+id/tvRealtime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:translationY="-50dp"
            android:background="@drawable/bg_panel_rounded"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:text="0.00 м"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Нижняя панель кнопок -->
    <LinearLayout
        android:id="@+id/bottomPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="16dp"
        android:orientation="horizontal"
        android:weightSum="4"
        android:background="@drawable/bg_panel_rounded"
        android:padding="10dp"
        android:elevation="8dp">

        <ImageButton
            android:id="@+id/btnUndo"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="1"
            android:backgroundTint="#132238"
            android:src="@android:drawable/ic_menu_revert"
            android:tint="#F6FAFF" />

        <Button
            android:id="@+id/btnPlace"
            android:layout_width="0dp"
            android:layout_height="56dp"
            android:layout_weight="3"
            android:layout_marginStart="10dp"
            android:backgroundTint="#46E0A1"
            android:textColor="#000000"
            android:text="Зафиксировать"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>